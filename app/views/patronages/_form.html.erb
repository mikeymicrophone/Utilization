<%= form_for(@patronage) do |f| %>
  <% if @patronage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patronage.errors.count, "error") %> prohibited this patronage from being saved:</h2>

      <ul>
      <% @patronage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :characteristic %><br />
    <%= f.text_field :characteristic %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
